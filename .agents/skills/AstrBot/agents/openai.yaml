interface:
  display_name: "AstrBot"
  short_description: "Repo-specific patterns and workflows for AstrBot"
  default_prompt: "Use the AstrBot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true